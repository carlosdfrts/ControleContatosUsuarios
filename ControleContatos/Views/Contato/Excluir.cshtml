@model ContatoModel
@{
    ViewData["Title"] = "Confirmação exclusão do contato";
}

<div class="text-center">
    <h3>Realmente deseja apagar o contato @Model.Nome ?</h3>
    <br />
</div>

<a role="button" class="btn btn-danger w-100 mb-2" asp-controller="Contato" asp-action="Apagar" asp-route-id="@Model.Id">Confirmar exclusão</a>
<a role="button" class="btn btn-primary w-100 mb-2" asp-controller="Contato" asp-action="Index" >Cancelar exclusão</a>